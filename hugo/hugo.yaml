baseURL: "https://example.org/"
languageCode: "en-us"
title: "Hugo Bibliography via Zotero API"

# ignore hugo cache
# to get the latest zotero data every time
caches:
  getjson:
    maxAge: 0
  getresource:
    maxAge: 0
# Use a special config file to store your zotero API key
# ignore this file in .gitignore to prevent sharing it
# module:
#   imports:
#     - path: "zotero.yaml"
